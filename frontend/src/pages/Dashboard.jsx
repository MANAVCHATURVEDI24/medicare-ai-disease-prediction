import { useNavigate } from "react-router-dom";
import { useAppContext } from "../context/AppContext";
import PredictionCard from "../components/prediction/PredictionCard";

function Dashboard() {
  const navigate = useNavigate();
  const { predictions, setReportData, setQuestions, setPredictions } = useAppContext();

  const handleStartOver = () => {
    setReportData(null);
    setQuestions([]);
    setPredictions(null);
    navigate("/upload");
  };

  const predictionList = predictions ? Object.values(predictions) : [];

  /* ── Empty State ───────────────────────────────────── */
  if (!predictions || predictionList.length === 0) {
    return (
      <div className="mx-auto max-w-xl py-16 text-center">
        <div className="mb-6 flex justify-center">
          <div className="flex h-20 w-20 items-center justify-center rounded-2xl bg-blue-50 text-blue-600 shadow-sm border border-blue-100">
            <svg className="h-10 w-10" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={1.5} d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
            </svg>
          </div>
        </div>

        <h1 className="text-2xl font-bold tracking-tight text-slate-800">
          No Prediction Results Yet
        </h1>
        <p className="mt-3 text-slate-500 text-sm leading-relaxed max-w-md mx-auto">
          Upload your medical lab report to get instant AI-powered risk evaluations for Diabetes, Liver Disease, and Cardiovascular conditions.
        </p>

        <div className="mt-8 flex justify-center">
          <button
            type="button"
            onClick={() => navigate("/upload")}
            className="flex items-center gap-2 rounded-xl bg-blue-600 px-6 py-3 text-sm font-semibold text-white shadow-sm transition-all hover:bg-blue-700 hover:shadow"
          >
            <svg className="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12" />
            </svg>
            Upload Lab Report
          </button>
        </div>
      </div>
    );
  }

  /* ── Populated Dashboard ────────────────────────────── */
  return (
    <div className="mx-auto max-w-6xl">

      {/* Header section with title + Start Over button */}
      <div className="mb-8 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between border-b border-slate-200 pb-6">
        <div>
          <h1 className="text-3xl font-bold tracking-tight text-slate-800">
            Prediction Dashboard
          </h1>
          <p className="mt-1 text-slate-500 text-sm">
            AI-assisted risk assessment based on your lab parameters.
          </p>
        </div>

        <button
          type="button"
          onClick={handleStartOver}
          className="inline-flex items-center justify-center gap-2 rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-semibold text-slate-700 transition-colors hover:bg-slate-50 hover:text-slate-900 shrink-0"
        >
          <svg className="h-4 w-4 text-slate-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />
          </svg>
          Start Over
        </button>
      </div>

      {/* Prediction Cards Grid */}
      <div className="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
        {predictionList.map((res, index) => (
          <PredictionCard key={res.disease || index} result={res} />
        ))}
      </div>

      {/* Single Medical Disclaimer section at the bottom */}
      <div className="mt-10 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">
        <div className="flex items-start gap-3">
          <div className="mt-0.5 shrink-0 text-amber-500">
            <svg className="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
            </svg>
          </div>
          <div>
            <h4 className="text-sm font-semibold text-slate-800">Medical Disclaimer</h4>
            <p className="mt-1 text-xs text-slate-500 leading-relaxed">
              These prediction results are generated by machine learning models trained on health datasets for preliminary screening purposes only. They do not constitute formal medical diagnoses or clinical advice. Always consult a qualified healthcare professional regarding any health concerns or before making medical decisions.
            </p>
          </div>
        </div>
      </div>

    </div>
  );
}

export default Dashboard;